# Fichier dataset.json : liste des stations services

Le fichier contenu dans ce dossier porte le nom `dataset.json`.

Il est à mettre à jour dynamiquement en utilisant l'url /api/refresh-data-carburant ; il n'est donc pas contenu dans le dossier téléchargeable sur Github.